export const metadata = {
  title: 'YouTube Auto Publisher',
  description: 'Generate and publish YouTube videos automatically using AI',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
